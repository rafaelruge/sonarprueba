import { MatchSubmenuPipe } from './match-submenu.pipe';

describe('MatchSubmenuPipe', () => {
  it('create an instance', () => {
    const pipe = new MatchSubmenuPipe();
    expect(pipe).toBeTruthy();
  });
});
